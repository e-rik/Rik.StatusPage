<Project>

  <PropertyGroup>
    <Authors>Janno Põldma;Jakko Sikkar</Authors>
    <Copyright>Copyright © 2018 Janno Põldma and Jakko Sikkar</Copyright>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)' == 'Release' ">
    <AssemblyOriginatorKeyFile>..\..\Rik.StatusPage.pfx</AssemblyOriginatorKeyFile>
    <SignAssembly>true</SignAssembly>
    <PublicSign Condition=" '$(OS)' != 'Windows_NT' ">true</PublicSign>
  </PropertyGroup>

</Project>